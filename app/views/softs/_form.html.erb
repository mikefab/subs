<%= form_for(@soft) do |f| %>
  <% if @soft.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@soft.errors.count, "error") %> prohibited this soft from being saved:</h2>

      <ul>
      <% @soft.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text %>
  </div>



  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
